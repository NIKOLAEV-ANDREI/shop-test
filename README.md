# Тестовое задание Prestashop

Исходный код Prestashop - https://github.com/prestashop/PrestaShop-1.6

### Запуск

1. docker compose up -d
2. http://localhost:8085/
3. Выполняем установку, параметры подключения к БД можно посмотреть в docker-compose.yml
4. Удаляем в контейнере папку /var/www/html/install


## Задание

## Цель

Разработать небольшой модуль для **PrestaShop 1.6**, который добавляет возможность хранить внутренние комментарии менеджеров к заказу.

Задание максимально приближено к типовым задачам по поддержке существующего интернет-магазина: необходимо разобраться в структуре проекта, механизмах расширения PrestaShop и работе с базой данных.

Глубокое предварительное знание PrestaShop не требуется. Можно пользоваться документацией, поиском, IDE и другими привычными инструментами разработки.

## Задача

Необходимо реализовать модуль:

```text
managercomment
```

Модуль должен добавлять к заказу внутренние комментарии сотрудников магазина.

### Функциональные требования

На странице просмотра заказа в административной панели PrestaShop должен появиться блок примерно следующего вида:

```text
Комментарии менеджеров

Алексей Федоров
17.08.2026 14:35
Связаться с клиентом перед отправкой.

[ Редактировать ]

Иван Петров
17.08.2026 15:10
Клиент подтвердил адрес доставки.

--------------------------------------------

Новый комментарий

┌─────────────────────────────────────────────┐
│                                             │
│ Текст комментария                           │
│                                             │
└─────────────────────────────────────────────┘

[ Добавить комментарий ]
```

Точный внешний вид интерфейса остается на усмотрение кандидата.

Комментарии предназначены только для сотрудников магазина и не должны отображаться покупателю.

Необходимо реализовать:

1. Возможность добавлять **несколько комментариев к одному заказу**.
2. Сохранение автора каждого комментария.
3. Отображение имени сотрудника, добавившего комментарий.
4. Сохранение даты создания комментария.
5. Возможность редактирования комментария.
6. Пользователь может редактировать **только собственные комментарии**.
7. Пользователь не должен иметь возможности редактировать комментарии других сотрудников.
8. После повторного открытия заказа все ранее добавленные комментарии должны корректно отображаться.
9. Комментарии разных заказов не должны смешиваться.

## Валидация комментария

Валидация должна обязательно выполняться **на стороне backend**.

Требования к длине комментария:

* минимальная длина — **5 символов**;
* максимальная длина — **500 символов**.

Если комментарий не соответствует этим требованиям, он не должен сохраняться.

Пользователь должен получить понятное сообщение об ошибке.

Frontend-валидацию также можно добавить, но она не должна заменять backend-валидацию.

Следует учитывать как минимум следующие случаи:

```text
"abc"
```

Такой комментарий не должен сохраниться, так как он короче 5 символов.

Комментарий длиной более 500 символов также не должен сохраниться.

Продумайте поведение для строки, состоящей только из пробелов.

## Работа с пользователями

Для каждого комментария необходимо определить текущего авторизованного сотрудника PrestaShop и сохранить информацию об авторе.

В интерфейсе рядом с комментарием должно отображаться имя сотрудника.

Например:

```text
Дмитрий Петров
17.08.2026 14:35

Связаться с клиентом перед отправкой.
```

Способ хранения информации об авторе остается на усмотрение кандидата.

При этом следует учитывать, что имя сотрудника в системе в будущем может измениться.

Если это влияет на выбранную структуру хранения данных, кратко объясните принятое решение.

## Права на редактирование

При редактировании комментария необходимо проверять права **на backend**, а не только скрывать кнопку в интерфейсе.

Например, если пользователь вручную отправит запрос на изменение чужого комментария, сервер должен отклонить этот запрос.

Условие:

```text
Автор комментария == текущий пользователь
```

только тогда комментарий может быть изменен.

Продумайте, какое сообщение или HTTP-поведение должно возникать при попытке изменить чужой комментарий.

## Работа с базой данных

При установке модуль должен создать необходимую таблицу или таблицы.

Пример возможной структуры:

```text
manager_comment

id
id_order
id_employee
comment
date_add
date_upd
```

Использовать именно такую структуру необязательно. Можно выбрать другую схему, если она кажется более подходящей.

Следует учитывать, что:

* один заказ может иметь много комментариев;
* один сотрудник может оставить много комментариев;
* комментарий принадлежит одному заказу;
* комментарий имеет одного автора.

При необходимости добавьте индексы и ограничения, которые считаете полезными.

## Ограничения

Не следует изменять core-файлы PrestaShop, если задачу можно решить штатными механизмами расширения платформы.

Допускается использование:

* модулей;
* hooks;
* overrides, если без них реализация существенно усложняется.

Если используется override или другое решение, влияющее на стандартное поведение приложения, кратко объясните причину такого выбора.

Не требуется реализовывать сложный дизайн. Достаточно простого и функционального интерфейса, соответствующего административной панели PrestaShop.

## Удаление модуля

Продумайте поведение при удалении модуля.

Допустимы оба варианта:

* удалить созданные таблицы и данные;
* сохранить данные после удаления модуля.

Выберите один вариант и кратко объясните решение.

## SQL-задание

Необходимо подготовить SQL-запросы:

1. Получить последние 100 комментариев менеджеров.

Для каждого комментария необходимо получить:

* ID заказа;
* дату заказа;
* имя клиента;
* сумму заказа;
* текст комментария;
* имя сотрудника, оставившего комментарий;
* дату создания комментария;
* дату последнего изменения комментария.

2. Получить топ-5 заказов, в которых оставлено больше всего комментариев.

   Для каждого заказа необходимо вывести:

   * ID заказа;
   * количество комментариев;
   * дату первого комментария;
   * дату последнего комментария.

3. Получить последние 3 заказа, в которых нет ни одного комментария менеджеров.

Запросы можно сохранить, например, в:

```text
sql/order_comments.sql
```

Если считаете, что для эффективной работы запроса нужны дополнительные индексы, укажите их.

## Что необходимо предоставить

В результате в репозитории должны находиться:

```text
managercomment/
    ...

README.md

sql/
    order_comments.sql
```

Модуль должен устанавливаться через административную панель PrestaShop или другим стандартным для PrestaShop способом.

Добавьте в `README.md` краткое описание:

* как установить модуль;
* как проверить его работу;
* какие архитектурные решения были приняты;
* как реализована проверка авторства при редактировании;
* как реализована backend-валидация;
* какие известные ограничения есть у реализации.

## Что будет оцениваться

Основное внимание будет уделяться не знанию конкретных методов PrestaShop наизусть, а подходу к решению задачи.

Будут оцениваться:

* способность разобраться в незнакомом существующем проекте;
* структура и читаемость кода;
* корректность работы с PHP;
* понимание механизмов PrestaShop;
* работа с SQL и базой данных;
* корректное определение текущего пользователя;
* проверка прав на редактирование на backend;
* backend-валидация входных данных;
* обработка ошибок и граничных случаев;
* отсутствие лишних изменений в ядре системы;
* архитектурные решения;
* способность объяснить принятые решения.

Использование документации и внешних источников разрешено.

## Необязательные улучшения

Если останется время, можно реализовать дополнительные улучшения, например:

* сохранение и редактирование комментариев через AJAX;
* отображение признака редактирования комментария;
* сортировку комментариев;
* пагинацию при большом количестве комментариев;
* удаление собственных комментариев;
* дополнительные проверки входных данных;
* автоматические тесты.

Эти пункты не являются обязательными и не должны выполняться в ущерб основной функциональности.

Главное — рабочее, понятное и обоснованное решение основной задачи.
